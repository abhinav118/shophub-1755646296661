interface ShoppingCartIconProps {
  itemCount?: any
  onClick?: any
}

export default function ShoppingCartIcon({ itemCount, onClick }: ShoppingCartIconProps) {
  return (
    <div className="">
      {/* ShoppingCartIcon component content */}
    </div>
  )
}