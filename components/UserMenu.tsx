interface UserMenuProps {
  user?: any
  menuItems?: any
}

export default function UserMenu({ user, menuItems }: UserMenuProps) {
  return (
    <div className="">
      {/* UserMenu component content */}
    </div>
  )
}